# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# Environment (never commit secrets)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
__MACOSX/
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Workspace package builds + local file storage (uploads only — keep data/*.ts mocks in git)
packages/*/dist/
data/files/

# Local / EC2-style dumps created under repo during ops-console local runs
/backups/

