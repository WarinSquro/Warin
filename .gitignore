# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# Environment (never commit secrets)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
__MACOSX/
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Workspace package builds + local file storage (uploads only — keep data/*.ts mocks in git)
packages/*/dist/
data/files/

# Prisma (generated client lives in node_modules; local DB volumes if added later)
server/node_modules/
